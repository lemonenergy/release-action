import { Header, CommitResult } from './types';
export declare function parseHeader(header: string): Header;
export declare function stringifyHeader(header: Header): string;
export declare function validateHeader(header: Header): CommitResult;
export declare function checkHeader(header: Header, caseSensitive?: boolean): Header;
